# 新功能：无法访问书签自动分类

## ✨ 功能说明

**问题**：
- 有些书签链接已经失效（404、域名过期）
- 有些网站无法访问（超时、拒绝连接）
- 这些书签在重新识别时会浪费时间

**解决方案**：
- ✅ 自动检测无法访问的书签
- ✅ 归入 **"待确认 / 无法访问"** 分类
- ✅ 显示具体原因（HTTP 404、超时等）
- ✅ 方便用户统一处理（删除或修复）

---

## 🎯 工作原理

**重新识别流程**：

```
1. 关键词识别（快速） ✅
   ↓
2. 增强分类（访问页面）
   ├─ 访问成功 → 分析内容 → 分类 ✅
   └─ 访问失败 → 归入"无法访问" ⚠️
```

**无法访问的原因**：
- HTTP 404（页面不存在）
- HTTP 403（禁止访问）
- HTTP 500（服务器错误）
- 超时（网络问题）
- 域名无法解析
- 其他错误

---

## 📊 分类结果

**最终结构预览中会出现**：

```
📁 待确认
  └─ 📁 无法访问 (X 个)
      ├─ 🔗 某个失效链接 (HTTP 404)
      ├─ 🔗 超时网站 (访问失败)
      └─ 🔗 ...
```

**信息显示**：
- 原因：HTTP 404 / 超时 / 域名无法解析
- 置信度：0.85（表示确定无法访问）
- 操作：move（会被移动到这个分类）

---

## 🚀 使用场景

### 场景 1：清理失效书签
```
1. 重新识别待确认
2. 查看"无法访问"分类
3. 确认这些是失效链接
4. 执行整理方案 → 失效书签被移到"待确认/无法访问"文件夹
5. 手动删除这个文件夹
```

### 场景 2：标记待修复书签
```
1. 查看"无法访问"分类
2. 某些书签只是临时无法访问
3. 记录下来，稍后修复
```

---

## 📝 日志示例

**重新识别过程中**：
```
[重新识别] 开始增强分类（访问页面）：19 个书签
  └─ 某网站文章 - 无法访问 (HTTP 404)
  └─ 过期域名 - 无法访问 (域名无法解析)
[重新识别] 已保存 19 条增强分类结果
```

---

## 🎨 效果对比

### 修改前
```
无法访问的书签：
- 仍然显示在"待确认"
- 没有明确标识
- 无法批量处理
```

### 修改后
```
无法访问的书签：
✅ 单独归类到"待确认/无法访问"
✅ 显示失效原因
✅ 方便批量删除
```

---

## 💡 建议

**对于"无法访问"的书签**：

1. **立即删除**（如果确定不需要）
   - HTTP 404、域名过期的书签
   - 已经很久没用的链接

2. **暂时保留**（如果可能恢复）
   - 服务器临时维护
   - 网络问题导致的超时
   - 可能只是暂时失效

3. **尝试修复**
   - 搜索新的链接
   - 使用互联网档案馆（Wayback Machine）
   - 找到替代资源

---

## 🔧 技术细节

**判断逻辑**：
```javascript
const pageInfo = await fetchBookmarkPageInfo(bookmark);

if (!pageInfo.ok) {
  // 无法访问
  item.categoryPath = ['待确认', '无法访问'];
  item.confidence = 0.85;
  item.reason = `无法访问 (${pageInfo.error})`;
}
```

**可能的错误信息**：
- `HTTP 404` - 页面不存在
- `HTTP 403` - 禁止访问
- `HTTP 500` - 服务器错误
- `访问失败` - 网络超时
- `非 HTML 页面` - PDF、图片等
- `域名无法解析` - DNS 错误

---

## ✅ 立即测试

### 第 1 步：重新加载扩展
```
chrome://extensions/ → 重新加载
```

### 第 2 步：重新识别
```
点击 "重新识别待确认"
```

### 第 3 步：查看日志
如果有无法访问的书签，会看到：
```
  └─ xxx - 无法访问 (HTTP 404)
```

### 第 4 步：查看分类
在"最终结构预览"中应该看到：
```
📁 待确认
  └─ 📁 无法访问 (X 个)
```

---

**现在请重新加载扩展测试！** 🎯

无法访问的书签会被自动分类了。
